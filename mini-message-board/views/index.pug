extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/new" class="link") New Post

  div(class="comments")
    each message in messages
      div(class="comment")
        h2 #{message.user}
        p #{message.text}
        p Submitted: #{message.added} 
